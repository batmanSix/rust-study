fn main() {
    // 整型 有符号整型 无符号整型 （有符号类型signed 无符号 signed）
    // 按字节划分 1字节，2字节，4字节，8字节，16字节， 1字节=8位
    let price = 100;
    let price2: u32 = 100; //  无符号
    let price3: i32 = -300; // 有符号
    let price4: isize = 400; // 有符号
    let price5: usize = 500; // 无符号
}
